# Servidor de Desarrollo

Inicia el servidor de desarrollo local:

1. **Iniciar servidor**: Ejecuta `npm run dev` en segundo plano
2. **Esperar**: Espera 3 segundos para que el servidor se levante
3. **Abrir navegador**: Usa el MCP de Chrome DevTools para abrir http://localhost:5173 en el navegador
4. **Confirmar**: Informa al usuario que el servidor está corriendo y la URL donde puede acceder

Si hay errores al iniciar, muestra el mensaje de error al usuario.
